import 'package:flutter/material.dart';

class ProfilePagetest extends StatelessWidget {
  const ProfilePagetest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Laporan Page", style: TextStyle(fontSize: 20)),
    );
  }
}
